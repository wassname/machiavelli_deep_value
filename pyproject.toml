[project]
name = "machiavelli-deep-value"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.4.1",
    "loguru>=0.7.3",
    "openai>=2.15.0",
    "polars>=1.36.1",
]

[dependency-groups]
dev = ["pytest>=9.0.2"]
